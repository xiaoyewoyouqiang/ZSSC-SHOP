spring:
  application:
    name: shop-zuul

server:
  port: 80
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7777/eureka/

zuul:
  sensitive-headers:
ribbon: # 设置zuul超时时间
  ReadTimeout: 3000
  ConnectTimeout: 3000
