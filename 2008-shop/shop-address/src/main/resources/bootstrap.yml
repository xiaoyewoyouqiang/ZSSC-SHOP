eureka:
  client:
    service-url:
      defaultZone: http://localhost:7777/eureka
spring:
  application:
    name: shop-address
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/2008-shop?allowMultiQueries=true
    driver-class-name: com.mysql.jdbc.Driver
server:
  port: 8012
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml